Untuk menjalankanya harus dari folder "backend" 
1. flask run app.py

# CARA RUN DEV - LIVE RELOAD
    
```bash
docker compose -f docker-compose.yml up --build -d
```

# Cara ngrok

```bash
ngrok http http://localhost:5010
```